package io.github.excu101.vortex.provider.storage

import io.github.excu101.pluginsystem.provider.Managers
import io.github.excu101.vortex.data.PathItem

class StorageOperationProvider {

    fun runOperation(
        name: String,
        vararg items: PathItem,
    ) {
        Managers.Operation
    }

}